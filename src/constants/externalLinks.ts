/** Stored on file_sorter_items.attachment_mime_type for cloud links (no downloadable file). */
export const EXTERNAL_FILE_LINK_MIME = 'application/vnd.external-file-link';

export const GOOGLE_DRIVE_HOSTS = ['drive.google.com', 'docs.google.com'];
